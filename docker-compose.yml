version: '3.8'

services:

  backend:
    build: ./backend
    container_name: voyage-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: voyage-frontend
    ports:
      - "8501:8501"
    depends_on:
      - backend